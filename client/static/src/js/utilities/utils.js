// //Contains utility functions that can be reused across the project, such as helper functions for DOM manipulation, event handling, or general-purpose algorithms.


// export let selectObjectById = (board, id) => {
//     for (let row of board) {
//       for (let tile of row) {
//         if (typeof tile === 'object' && tile.id === id) {
//           return tile;
//         }
//       }
//     }
//     return null;
//   };